$("[data-role$=receiver] form .busy").hide()

$("[data-role=product_couples_receiver] .panel-title .size").text(<%= @product.product_couples.count %>)
$("[data-role=extra_categories_receiver] .panel-title .size").text(<%= @product.extra_categories.without(@product.parent_id).count %>)
